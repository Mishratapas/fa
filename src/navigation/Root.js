import { NavigationContainer } from '@react-navigation/native'

import Auth from './auth'

const Root = () => {
  return (
    <NavigationContainer>
      <Auth />
    </NavigationContainer>
  )
}

export default Root
